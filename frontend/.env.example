# Frontend environment variables (Vite)
# Copy to .env.local for local development

# API base URL — leave empty when using Vite dev proxy or Vercel same-origin /api
# Set to backend URL when frontend and API are deployed separately
VITE_API_URL=

# Example for separate backend deployment:
# VITE_API_URL=https://dishwipe-api.onrender.com
